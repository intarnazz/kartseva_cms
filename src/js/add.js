"use strict";

import { ref } from "./utils.js";

ref("addItemForm").addEventListener("submit", (e) => {
  e.preventDefault;
  const name = ref("name");
  const category = ref("category");
  const description = ref("description");
  const units = ref("units");
  const discount = ref("discount");
  const count = ref("count");
  const price = ref("price");
  const category = ref("category");
});
